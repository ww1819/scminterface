<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scminterface.framework.web.mapper.SbTenantSettingMapper">

    <select id="selectValueByTenantAndKey" resultType="java.lang.String">
        select setting_value from sb_tenant_setting
        where tenant_id = #{tenantId} and setting_key = #{settingKey}
          and ifnull(del_flag, 0) = 0
        limit 1
    </select>

    <insert id="insertSetting">
        insert into sb_tenant_setting (
            id, tenant_id, setting_key, setting_value, remark, del_flag,
            create_by, create_time, update_by, update_time
        ) values (
            replace(uuid(), '-', ''), #{tenantId}, #{settingKey}, #{settingValue}, #{remark}, 0,
            'scminterface', now(), 'scminterface', now()
        )
    </insert>

    <update id="updateSettingValue">
        update sb_tenant_setting
        set setting_value = #{settingValue},
            remark = #{remark},
            update_by = 'scminterface',
            update_time = now()
        where tenant_id = #{tenantId} and setting_key = #{settingKey} and ifnull(del_flag, 0) = 0
    </update>
</mapper>
